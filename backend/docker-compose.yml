version: '3.9'  # Docker Compose version

services:
  auth-service:
    build: ./auth-service  # Path to the Dockerfile
    ports:
      - "3001:3001"  # Expose port 3000 on host
    env_file:
      - .env
    #networks:
    #  - microservices-network

  users-service:
    build: ./users-service
    ports:
      - "3002:3002"
    env_file:
      - .env
    #environment:
    #  - MONGODB_URI="mongodb+srv://admin:Turiba333@cluster0.wkfnyom.mongodb.net/LMS?retryWrites=true&w=majority&appName=Cluster0"
    #  - JWT_SECRET=""
    #  - PORT="3002"
    #  - NODE_ENV=development
    #networks:
    #  - microservices-network
